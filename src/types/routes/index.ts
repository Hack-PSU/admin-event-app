export enum RootRoute {
    Auth = "Auth",
    HomeRoute = "Home"
}

export enum HomeRoute {
    Main = "Main",
    CodeRoute = "Code"
}

export enum CodeRoute {
    Scan = "Scan",
    Code = "Code"
}