export enum RootRoute {
    Auth = "Auth",
    HomeRoute = "Home"
}

export enum HomeRoute {
    Admin = "Admin",
    CodeRoute = "CodeRouter"
}

export enum CodeRoute {
    Scan = "Scan",
    Code = "Code"
}